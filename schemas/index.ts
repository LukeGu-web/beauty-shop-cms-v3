import navbar from './navbar'
import navbarCategory from './navbar-section'
import category from './category'
import product from './product'

export const schemaTypes = [category, product, navbar, navbarCategory]
